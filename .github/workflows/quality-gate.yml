name: Quality Gate

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

concurrency:
  group: quality-gate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  static-and-unit:
    name: Static + Unit Regression
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      CI: true
      NEXT_TELEMETRY_DISABLED: 1
      # Prisma generate loads prisma.config.ts and requires a URL, but does not
      # connect to the database. Keep PR L1/L2 fully isolated from real DBs.
      DATABASE_URL: postgresql://ci:ci@localhost:5432/visutry_ci
      DATABASE_URL_UNPOOLED: postgresql://ci:ci@localhost:5432/visutry_ci

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: TypeScript gate
        run: npm run typecheck

      - name: Unit and regression gate
        run: npm run test:unit:ci
