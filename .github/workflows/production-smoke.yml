name: Production Smoke

on:
  push:
    branches: [main]
  schedule:
    - cron: '17 */6 * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: production-smoke
  cancel-in-progress: true

jobs:
  route-health:
    name: Production Route Health
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Wait briefly for main deployment propagation
        if: github.event_name == 'push'
        run: sleep 45

      # Vercel is the sole Next frontend owner. This gate fails the release if any
      # production Next HTML, /_next/static asset, or RSC/Flight response is served
      # by the Cloudflare Worker, or if any referenced /_next/static asset 404s
      # (the 2026-08-19 ChunkLoadError class of incident).
      - name: Run zero-AI production smoke + Next asset-integrity gate
        env:
          SMOKE_BASE_URL: https://www.visutry.com
          SMOKE_ATTEMPTS: '8'
          SMOKE_DELAY_MS: '15000'
        run: node scripts/production-smoke.mjs
