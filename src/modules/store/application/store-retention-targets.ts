/**
 * @deprecated Import from `@/lib/retention/tryon-retention-targets` instead.
 * Re-export kept for Store application barrel compatibility (ADR-007).
 */
export {
  isDeletableBlobRef,
  collectTryOnRetentionDeleteTargets,
} from '@/lib/retention/tryon-retention-targets'
