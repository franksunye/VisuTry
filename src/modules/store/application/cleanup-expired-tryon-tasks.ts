/**
 * @deprecated Import from `@/lib/retention/cleanup-expired-tryon-tasks` instead.
 * Re-export kept for Store application barrel compatibility (ADR-007).
 */
export {
  cleanupExpiredTryOnTasks,
  type CleanupExpiredTryOnTasksResult,
} from '@/lib/retention/cleanup-expired-tryon-tasks'
