{
  "buildCommand": "rm -rf .next node_modules/.cache && prisma migrate deploy && npm run build",
  "installCommand": "npm ci",
  "framework": "nextjs",
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
