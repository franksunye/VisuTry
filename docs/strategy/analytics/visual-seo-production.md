# VisuTry Visual SEO Production v1.0

**Status:** Active execution specification  
**Last updated:** 2026-08-03  
**Owner:** Growth / Product / Engineering  
**Parent strategy:** `docs/strategy/analytics/gtm.md` → Engine 2 — Visual Discovery  
**Scope:** Google Images, Search→Tool page visuals, image SEO, Pinterest-ready source assets, and Codex-assisted production/integration.

> This document is the single source of truth for the current Visual SEO image-production program. Do not create a parallel image plan in another document. If the asset list, naming rules, templates, or completion state changes, update this file.

---

## 1. Why this exists

VisuTry is a visual eyewear decision product, not a text-only content site. The public consumer path is:

> **Face Shape Detector → Glasses Advisor → Virtual Try-On → Frame Compare**

The Visual SEO program turns this workflow into original, indexable visual assets that can acquire users from Google Images and reinforce Search→Tool pages.

The program is intentionally **not** a generic AI-image content project. Every image must answer a concrete eyewear decision question and connect to an existing or approved acquisition page.

Primary distribution surfaces:

1. Google Images;
2. Search→Tool landing pages;
3. Pinterest derived assets;
4. selected Reddit / YouTube / editorial reuse where relevant.

---

## 2. Production target and current progress

### Total target

**182 production images**

### Current accepted progress

**20 / 182 accepted images**

- Images `VSEO-001` through `VSEO-020` are the accepted first 20.
- Contact sheets, batch overviews, accidental grids, wrong-topic generations, and cancelled generations do **not** count.
- The next canonical production item is `VSEO-021`.

### Planned composition

| Asset family | Planned count |
| --- | ---: |
| Core commercial / product-intent pages | 24 |
| Face Style owner pages | 18 |
| Face Shape explainer pages | 12 |
| Face × Frame combination pages | 64 |
| Gender / styling intent pages | 32 |
| Decision-question pages | 24 |
| Hub / navigation pages | 8 |
| **Total** | **182** |

The count is a production target, not an excuse to publish low-value images. If a generated asset fails QA, it is rejected and does not advance the counter.

---

## 3. Two-name system: source handoff vs public SEO filename

To prevent human/Codex handoff errors while keeping public filenames SEO-friendly, use two names.

### 3.1 Source handoff filename

When downloading an image generated by ChatGPT or another image model, save it locally as:

```text
VSEO-###__semantic-name.png
```

Example:

```text
VSEO-021__square-face-glasses-proportions.png
```

Rules:

- `VSEO-###` is the immutable production ID;
- use three digits (`001`, `021`, `182`);
- use a double underscore between the ID and semantic name;
- keep the source file as PNG unless the generator provides a lossless equivalent;
- never reuse an ID for a different concept.

### 3.2 Public SEO filename

Codex converts/exports the accepted asset to WebP and removes the production ID:

```text
square-face-glasses-proportions.webp
```

Public filenames must be:

- lowercase;
- English;
- hyphen-separated;
- semantically descriptive;
- stable after publication;
- free of generic names such as `image1`, `final`, `new`, `img`, or timestamps.

### Why two names

The production ID prevents us from losing sequence or confusing downloads. The semantic public filename remains clean for Google Images and page markup.

---

## 4. Local and public directories

### Local production inbox

Codex should use a local working folder inside the project:

```text
.local-assets/visual-seo/inbox/
```

This directory is a local handoff/workbench and should be gitignored if it is not already ignored.

Downloaded source examples:

```text
.local-assets/visual-seo/inbox/VSEO-021__square-face-glasses-proportions.png
.local-assets/visual-seo/inbox/VSEO-022__heart-face-frame-comparison.png
```

### Public optimized assets

Accepted optimized files should be stored under:

```text
public/images/seo/
```

Recommended subfolders:

```text
public/images/seo/core/
public/images/seo/face-shapes/
public/images/seo/style/
public/images/seo/guides/
public/images/seo/decisions/
public/images/seo/hubs/
```

Codex may refine subfolders if the existing repository has a stronger convention, but must keep one canonical location and update this document if the convention changes.

---

## 5. Standard output specification

### Master source

- preferred composition: **4:3**;
- target master resolution: **1600×1200 or higher**;
- minimum acceptable page asset: **1200×800 / 1200×900 depending on template**;
- source: PNG;
- deployed format: WebP;
- optional AVIF only if the existing image pipeline supports it cleanly;
- explicit `width` and `height` in page markup;
- no unnecessary transparency.

### Pinterest derivative

Pinterest vertical derivatives are **not included in the 182 master-image count**.

When needed, derive from the approved master:

```text
1000×1500
```

Do not generate a separate visual concept merely to satisfy Pinterest dimensions unless data supports it.

---

## 6. Visual language

All production images should feel like one VisuTry visual knowledge system.

### Required style

- white / pearl-white background;
- restrained blue accent;
- clean editorial / product-infographic composition;
- realistic portrait photography;
- front-facing or near-front-facing faces when fit/proportion is being explained;
- realistic glasses scale and placement;
- generous whitespace;
- minimal text inside the generated image;
- high information density without looking like an ad banner.

### Avoid

- heavy gradients;
- excessive fashion-editorial effects;
- neon / cyberpunk AI aesthetics;
- overly pink or playful cosmetic styling;
- unrealistic frame dimensions;
- generated fake UI that contradicts the real VisuTry product;
- generated VisuTry logos.

**Important:** Do not ask the image model to render the VisuTry logo. Generated logos have been inconsistent. If branding is needed, Codex/design code may overlay the real project logo after generation.

---

## 7. Six canonical visual templates

### Template A — Hero Recommendation

**Purpose:** Answer “what works for this face / intent?” immediately.

**Typical pages:** style owners, Face × Frame, gender/styling, commercial recommendation pages.

**Composition:**

- one primary realistic portrait;
- 1–3 frame directions or visual variants;
- one short title;
- up to three short supporting cues.

**Default size:** `1200×800`.

**Examples:**

- Best Glasses for Round Face;
- Rectangle Glasses for Round Face;
- Glasses for Oval Face Women.

---

### Template B — A/B Compare

**Purpose:** Support an actual decision between frame directions.

**Composition:**

- same person, same crop, same lighting;
- two to four frame candidates;
- symmetric layout;
- neutral comparison language.

**Default size:** `1200×800`.

**Language rule:** prefer `Strong option`, `Adds structure`, `Softer look`, `More lift`; avoid unsupported universal claims such as `perfect`, `best for everyone`, or arbitrary numerical scores.

---

### Template C — Watch For / Fit Check

**Purpose:** Explain frame width, brow alignment, lens depth, bridge, height, proportion, or common fit issues.

**Composition:**

- one main face or 2–3 aligned examples;
- restrained arrows / measurement lines;
- `Too narrow / Balanced / Too wide` or equivalent where useful;
- minimal explanatory text.

**Default size:** `1200×900`.

---

### Template D — Face Shape Diagram

**Purpose:** Help users understand visual face-shape characteristics.

**Composition:**

- front-facing portrait or simplified face illustration;
- forehead / cheekbone / jawline / length guides;
- one face shape per image unless the image is explicitly a comparison guide.

**Default size:** `1200×900`.

**Product language:** face-shape output is guidance, not a medical or biometric identity claim.

---

### Template E — Workflow / Product CTA

**Purpose:** Explain the decision flow without inventing fake product interfaces.

Canonical flow:

> Upload / Detect → Advisor → Try-On → Compare

**Composition:** 3–4 simple steps, real product screenshots only when supplied/available; otherwise use abstract UI-safe cards/icons.

**Default size:** `1200×800`.

---

### Template F — Decision Answer Card

**Purpose:** Answer concrete search questions such as:

- Do round glasses suit a round face?
- Should glasses cover your eyebrows?
- How wide should glasses be?

**Composition:** question → direct answer → visual evidence → one watch-for.

**Default size:** `1200×900`.

---

## 8. Generation protocol — mandatory

Image models have repeatedly tried to turn a batch request into one contact sheet. Therefore the generation protocol is strict.

### Rule 1 — One tool call = one production asset

Never ask the image model for:

- a batch overview;
- a contact sheet;
- a 2×5 grid;
- ten thumbnails;
- a production dashboard;
- a progress board.

A prompt must describe **only one final image**.

### Rule 2 — Batch size is operational, not visual

We may plan in batches of up to 10, but the model must still receive **10 separate image-generation calls**.

Example:

```text
Batch 03 = VSEO-021 ... VSEO-030
```

This means ten separate generations, not one request containing ten assets.

### Rule 3 — Reject without advancing

Reject an output if it:

- becomes a collage/contact sheet;
- depicts the wrong face shape;
- uses the wrong frame family;
- contains broken or misleading text;
- creates inconsistent/fictional VisuTry branding;
- makes the glasses obviously unrealistic;
- fails the intended search question.

Rejected assets do not advance the production counter.

### Rule 4 — Keep image text short

Prefer HTML page text around the image over long model-rendered paragraphs.

Inside-image copy should normally be limited to:

- title;
- 2–4 labels;
- one short comparison cue.

### Rule 5 — Never fabricate product UI

If the image requires an exact VisuTry screen, use a real screenshot or create the composition in code/design tooling. Image generation should not invent product screens that look authoritative but are inaccurate.

---

## 9. Production handoff workflow with Desktop Codex

### Step A — Determine next asset

Read this document and find the lowest `VSEO-###` not marked accepted.

Current next item:

```text
VSEO-021
```

### Step B — Generate one image

The generation prompt must include:

- production ID;
- semantic filename;
- target website URL;
- template A–F;
- single visual purpose;
- required face shape / frame family;
- 4:3 composition;
- visual-language constraints;
- explicit prohibition on grids/contact sheets/batch overviews.

### Step C — User download

Save the generated image to:

```text
.local-assets/visual-seo/inbox/VSEO-###__semantic-name.png
```

### Step D — Codex QA

Codex checks:

1. source file exists;
2. ID matches the manifest;
3. image dimensions are usable;
4. image topic matches page intent;
5. no duplicate destination filename exists;
6. the public page exists or is explicitly approved in the GTM plan.

Visual/factual quality still requires human review; Codex must not infer that a file is correct merely because its filename matches.

### Step E — Optimize

Codex converts to WebP, strips the production ID, and writes the optimized file under `public/images/seo/...`.

### Step F — Integrate

Codex updates the relevant page/config with:

- image `src`;
- descriptive `alt`;
- optional caption;
- explicit dimensions;
- surrounding explanatory copy;
- appropriate internal product CTA.

### Step G — SEO integration

Codex verifies:

- page is indexable;
- canonical is correct;
- image is crawlable;
- image URL is included in image sitemap if the project uses a dedicated image sitemap;
- no image is placed only behind client interaction with no discoverable markup;
- no duplicate image is assigned as the primary visual to many unrelated pages.

### Step H — Build and record

Run the project checks/build appropriate to the repository, then update the progress section in this document.

---

## 10. Accepted production IDs 001–020

These are the canonical handoff names for the first 20 accepted images. If a local downloaded filename differs, rename it to this convention before Codex integration.

| ID | Source handoff filename | Public filename | Primary page owner / intent |
| --- | --- | --- | --- |
| VSEO-001 | `VSEO-001__ai-face-shape-detector-example.png` | `ai-face-shape-detector-example.webp` | `/en/face-shape-detector` |
| VSEO-002 | `VSEO-002__common-face-shapes-guide.png` | `common-face-shapes-guide.webp` | `/en/face-shape-detector` |
| VSEO-003 | `VSEO-003__what-is-my-face-shape-result.png` | `what-is-my-face-shape-result.webp` | `/en/what-is-my-face-shape` |
| VSEO-004 | `VSEO-004__what-glasses-suit-my-face-guide.png` | `what-glasses-suit-my-face-guide.webp` | `/en/what-glasses-suit-my-face` |
| VSEO-005 | `VSEO-005__virtual-glasses-try-on-before-after.png` | `virtual-glasses-try-on-before-after.webp` | `/en/virtual-glasses-try-on` |
| VSEO-006 | `VSEO-006__compare-four-glasses-frames.png` | `compare-four-glasses-frames.webp` | `/en/compare-glasses-frames` |
| VSEO-007 | `VSEO-007__ai-glasses-advisor-analysis.png` | `ai-glasses-advisor-analysis.webp` | `/en/ai-glasses-advisor` |
| VSEO-008 | `VSEO-008__best-glasses-for-round-face.png` | `best-glasses-for-round-face.webp` | `/en/style/round-face` |
| VSEO-009 | `VSEO-009__best-glasses-for-oval-face.png` | `best-glasses-for-oval-face.webp` | `/en/style/oval-face` |
| VSEO-010 | `VSEO-010__rectangle-glasses-round-face-01-hero.png` | `rectangle-glasses-round-face-01-hero.webp` | `/en/glasses-guide/best-rectangle-glasses-for-round-face` |
| VSEO-011 | `VSEO-011__best-glasses-for-square-face.png` | `best-glasses-for-square-face.webp` | `/en/style/square-face` |
| VSEO-012 | `VSEO-012__best-glasses-for-heart-face.png` | `best-glasses-for-heart-face.webp` | `/en/style/heart-face` |
| VSEO-013 | `VSEO-013__best-glasses-for-diamond-face.png` | `best-glasses-for-diamond-face.webp` | `/en/style/diamond-face` |
| VSEO-014 | `VSEO-014__best-glasses-for-oblong-face.png` | `best-glasses-for-oblong-face.webp` | `/en/style/oblong-face` |
| VSEO-015 | `VSEO-015__round-face-frame-comparison.png` | `round-face-frame-comparison.webp` | `/en/style/round-face` |
| VSEO-016 | `VSEO-016__oval-face-frame-comparison.png` | `oval-face-frame-comparison.webp` | `/en/style/oval-face` |
| VSEO-017 | `VSEO-017__square-face-frame-comparison.png` | `square-face-frame-comparison.webp` | `/en/style/square-face` |
| VSEO-018 | `VSEO-018__glasses-eyebrow-alignment.png` | `glasses-eyebrow-alignment.webp` | decision intent: eyebrows / frame height |
| VSEO-019 | `VSEO-019__how-wide-should-glasses-be.png` | `how-wide-should-glasses-be.webp` | decision intent: frame width |
| VSEO-020 | `VSEO-020__cat-eye-glasses-round-face-01-hero.png` | `cat-eye-glasses-round-face-01-hero.webp` | Face × Frame: cat-eye × round face |

**Note:** Earlier conversational labels such as `B01-11` are superseded by the canonical `VSEO-###` numbering in this document. Codex should normalize filenames to `VSEO-###__...` in the local inbox.

---

## 11. Next canonical queue: VSEO-021 onward

The following items are the next approved queue and may be generated immediately one-by-one.

| ID | Source semantic name | Public filename | Page owner | Template |
| --- | --- | --- | --- | --- |
| VSEO-021 | `square-face-glasses-proportions` | `square-face-glasses-proportions.webp` | `/en/style/square-face` | C |
| VSEO-022 | `heart-face-frame-comparison` | `heart-face-frame-comparison.webp` | `/en/style/heart-face` | B |
| VSEO-023 | `heart-face-glasses-proportions` | `heart-face-glasses-proportions.webp` | `/en/style/heart-face` | C |
| VSEO-024 | `diamond-face-frame-comparison` | `diamond-face-frame-comparison.webp` | `/en/style/diamond-face` | B |
| VSEO-025 | `diamond-face-glasses-proportions` | `diamond-face-glasses-proportions.webp` | `/en/style/diamond-face` | C |
| VSEO-026 | `oblong-face-frame-comparison` | `oblong-face-frame-comparison.webp` | `/en/style/oblong-face` | B |
| VSEO-027 | `oblong-face-glasses-proportions` | `oblong-face-glasses-proportions.webp` | `/en/style/oblong-face` | C |
| VSEO-028 | `round-face-shape-characteristics` | `round-face-shape-characteristics.webp` | `/en/face-shapes/round` | D |
| VSEO-029 | `how-to-identify-round-face` | `how-to-identify-round-face.webp` | `/en/face-shapes/round` | D |
| VSEO-030 | `oval-face-shape-characteristics` | `oval-face-shape-characteristics.webp` | `/en/face-shapes/oval` | D |
| VSEO-031 | `how-to-identify-oval-face` | `how-to-identify-oval-face.webp` | `/en/face-shapes/oval` | D |
| VSEO-032 | `square-face-shape-characteristics` | `square-face-shape-characteristics.webp` | `/en/face-shapes/square` | D |
| VSEO-033 | `how-to-identify-square-face` | `how-to-identify-square-face.webp` | `/en/face-shapes/square` | D |
| VSEO-034 | `heart-face-shape-characteristics` | `heart-face-shape-characteristics.webp` | `/en/face-shapes/heart` | D |
| VSEO-035 | `how-to-identify-heart-face` | `how-to-identify-heart-face.webp` | `/en/face-shapes/heart` | D |
| VSEO-036 | `diamond-face-shape-characteristics` | `diamond-face-shape-characteristics.webp` | `/en/face-shapes/diamond` | D |
| VSEO-037 | `how-to-identify-diamond-face` | `how-to-identify-diamond-face.webp` | `/en/face-shapes/diamond` | D |
| VSEO-038 | `oblong-face-shape-characteristics` | `oblong-face-shape-characteristics.webp` | `/en/face-shapes/oblong` | D |
| VSEO-039 | `how-to-identify-oblong-face` | `how-to-identify-oblong-face.webp` | `/en/face-shapes/oblong` | D |
| VSEO-040 | `face-shape-photo-analysis-result` | `face-shape-photo-analysis-result.webp` | `/en/face-shape-detector` / `/en/what-is-my-face-shape` | D/E |

Do not reinterpret a failed/cancelled attempt as completion. `VSEO-021` remains next until an accepted Square Face Glasses Proportions image has been saved.

---

## 12. Deterministic naming for the remaining page families

The remainder of the 182-image program should be generated from the existing page registry/config rather than manually inventing routes.

### 12.1 Face Style owner pages

Code owner pattern:

```text
src/app/[locale]/(main)/style/[faceShape]/page.tsx
```

For each of `round`, `oval`, `square`, `heart`, `diamond`, `oblong`, the intended trio is:

```text
best-glasses-for-{face}-face.webp
{face}-face-frame-comparison.webp
{face}-face-glasses-proportions.webp
```

Some of these are already completed in `VSEO-008`–`VSEO-017` and the `VSEO-021` onward queue. Never generate a duplicate if the canonical public filename already exists and has passed QA.

### 12.2 Face Shape explainer pages

Page owner pattern:

```text
/en/face-shapes/{face}
```

For each of `round`, `oval`, `square`, `heart`, `diamond`, `oblong`:

```text
{face}-face-shape-characteristics.webp
how-to-identify-{face}-face.webp
```

These are Template D.

### 12.3 Face × Frame combination pages

Public page pattern:

```text
/en/glasses-guide/{slug}
```

Code owner pattern is the current dynamic glasses-guide detail route. **Codex must read the current route/config in the repository before integration and must not invent a slug that is not present or approved.**

For every approved Face × Frame slug, use four asset roles:

```text
{slug}-01-hero.webp
{slug}-02-why-it-works.webp
{slug}-03-watch-for.webp
{slug}-04-compare.webp
```

Template mapping:

- `01-hero` → A;
- `02-why-it-works` → A or D-like explanatory composition;
- `03-watch-for` → C;
- `04-compare` → B.

Examples already approved by strategy include combinations such as rectangle × round, cat-eye × round, cat-eye × oval, aviator × oval, aviator × square, browline × round/oval/diamond, oversized × oval/oblong, rimless × square, and related curated combinations present in the live configuration.

### 12.4 Gender / styling intent pages

For each approved gender/styling slug in the current glasses-guide registry, use:

```text
{slug}-01-overview.webp
{slug}-02-everyday.webp
{slug}-03-expressive.webp
{slug}-04-compare.webp
```

Template mapping:

- overview → A;
- everyday → A;
- expressive → A;
- compare → B.

Do not create gender/styling pages or imagery merely to fill a quota; only use the eight curated intents already approved/currently configured.

### 12.5 Decision-question pages

For each approved decision question, use four roles:

```text
{slug}-01-answer.webp
{slug}-02-correct-example.webp
{slug}-03-watch-for.webp
{slug}-04-compare.webp
```

Canonical decision topics include:

1. Do round glasses suit a round face?
2. Do aviators suit an oval face?
3. Are cat-eye glasses good for round faces?
4. Should glasses cover your eyebrows?
5. How wide should glasses be for my face?
6. How should glasses fit your face?

Template mapping:

- answer → F;
- correct example → A/F;
- watch-for → C;
- compare → B.

`VSEO-018` and `VSEO-019` already cover one visual in two of these decision clusters; Codex should assign them rather than generate a semantically duplicate image.

### 12.6 Hub pages

Owners:

```text
/en/glasses-for-face-shape
/en/glasses-guide
```

Planned hub assets:

```text
glasses-by-face-shape-overview.webp
face-shape-to-frame-map.webp
recommended-glasses-by-face-shape.webp
face-shape-glasses-decision-flow.webp

glasses-guide-overview.webp
face-frame-guide-map.webp
eyewear-decision-questions-guide.webp
visutry-eyewear-decision-workflow.webp
```

Hub visuals should be broad navigational/educational assets, not duplicate hero images from detail pages.

---

## 13. Page integration requirements

Every accepted image integrated into the site needs:

### `alt`

Describe what is visually present and why it matters.

Good:

```text
Rectangle, browline, round and cat-eye glasses compared on the same round face
```

Bad:

```text
Best glasses image
```

Do not keyword-stuff alt text.

### Caption / surrounding copy

The page should explain the image in ordinary HTML text. Important guidance should not exist only as pixels inside the image.

### Product continuation

Search/visual pages must continue into an actual VisuTry tool where relevant:

- Detector;
- Advisor;
- Try-On;
- Compare.

The Visual SEO asset is an acquisition/decision aid, not an endpoint.

---

## 14. Image SEO engineering checklist

Codex should verify or implement the following as part of integration:

- descriptive public filename;
- WebP optimization;
- explicit dimensions;
- responsive rendering;
- descriptive alt text;
- crawlable image URL;
- image sitemap support where the current implementation supports/uses it;
- meaningful surrounding text;
- correct canonical page;
- no accidental `noindex` on owner page;
- no lazy-loading implementation that makes critical visual undiscoverable in rendered markup;
- internal links into the owner page;
- no conflicting duplicate owner page for the same search intent.

Image performance must not materially regress acquisition-page Core Web Vitals.

---

## 15. QA rubric

An image is **Accepted** only when all required checks pass.

### Visual correctness

- intended face shape is visually plausible;
- intended frame family is recognizable;
- glasses are aligned realistically;
- left/right comparison uses a sufficiently consistent person/crop;
- proportions are not obviously distorted.

### Information correctness

- labels match the image;
- claims are guidance, not universal truth;
- no unsupported medical/biometric claims;
- no accidental brand/product claims;
- no fake pricing or feature UI.

### Brand/system correctness

- no fake VisuTry logo;
- visual style fits the white + restrained blue system;
- no unrelated batch labels or production metadata appear in the public visual;
- no contact sheet/grid unless the specific asset itself is intentionally a comparison visual.

### Technical correctness

- source and public filename match manifest;
- dimensions are sufficient;
- output is optimized;
- page integration builds successfully;
- alt/caption are present where required.

---

## 16. Codex execution algorithm

Desktop Codex can use the following loop:

```text
1. Read visual-seo-production.md.
2. Find next incomplete VSEO ID.
3. Check .local-assets/visual-seo/inbox for its source file.
4. If source is missing, report exact next filename and generation brief; do not skip ahead silently.
5. If source exists, inspect dimensions and mapping.
6. Convert/export optimized WebP to public/images/seo/... .
7. Integrate into the mapped owner page/config.
8. Add alt/caption and preserve Search→Tool CTA.
9. Update image sitemap/infrastructure if required.
10. Run tests/build.
11. Mark the item accepted in this document only after successful QA/build.
12. Continue to the next ID.
```

If Codex detects a discrepancy between this document and the current route registry, it should preserve the current canonical route and update this document rather than silently creating duplicate pages.

---

## 17. Progress ledger

| Range | Status | Notes |
| --- | --- | --- |
| VSEO-001–010 | ✅ Accepted | First generation batch |
| VSEO-011–020 | ✅ Accepted | Second generation batch; independent assets |
| VSEO-021–030 | ⏳ Next | Previous overview/contact-sheet attempts do not count |
| VSEO-031–182 | ⏳ Planned | Generate from canonical page/template rules |

### Current next action

> Generate **one independent image only** for `VSEO-021__square-face-glasses-proportions.png`, review it, save it to the local inbox, then let Codex integrate/advance.

---

## 18. Relation to the 10× GTM plan

Visual production is valuable only if it contributes to qualified discovery and product continuation.

Primary outcome metrics:

- Google Images impressions/clicks;
- landing-page qualified sessions;
- Visual-origin continuation into Detector / Advisor / Try-On / Compare;
- revenue / 1,000 qualified sessions where attribution is available.

The program should not be reported as successful merely because 182 images were generated. The business goal remains qualified traffic and paid-intent validation.
